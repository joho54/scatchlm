# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Backend (Python)
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/**/*.pyc
backend/.env
backend/.env.*
!backend/.env.prod.example
backend/uploads/
backend/.pytest_cache/

# Frontend (React Native / Expo)
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/expo-env.d.ts
mobile/ios/
mobile/android/
mobile/.metro-health-check*
mobile/*.tsbuildinfo

# Credentials
*.pem
*.p8
*.p12
*.key
*.mobileprovision
*.jks
ios-app/.build/

# Xcode 사용자별 상태 (per-user, 공유 불필요)
xcuserdata/
*.xcuserstate

# 기기 앱 컨테이너 덤프 (devicectl/Xcode로 추출 — 로컬 DB·스냅샷 포함, 대용량/PII)
*.xcappdata/

# 심볼릭 링크된 node_modules (cardnews는 screenshots의 node_modules를 symlink) — 디렉토리 패턴이 symlink를 못 잡음
docs/cardnews-instagram/node_modules

# PSI/메모리 분석 로컬 아티팩트 (sar 덤프 + 시각화 노트북) — 소스 아님, 로컬 분석용
mem7d*
